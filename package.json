{
  "name": "str8ts-web",
  "version": "1.0.0",
  "description": "Interactive Str8ts puzzle web app with a WASM-powered Rust solver and generator",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
